# Maximize Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# AUTHENTICATION OPTIONS (Choose ONE method)
# =============================================================================

# METHOD 1: Auto-Exchange from Authorization Code (EASIEST for production)
# Get the code from OAuth flow, server will auto-exchange for tokens
# Format: CODE#STATE (the full string from Anthropic OAuth page)
# MAXIMIZE_AUTHENTICATION_CODE=your-auth-code#state-string

# METHOD 2: Direct Token Input (if you already have tokens)
# Get these by running: ./maximize (CLI mode) -> Login
# Then read from: ~/.maximize/tokens.json
MAXIMIZE_ACCESS_TOKEN=sk-ant-your-access-token-here
MAXIMIZE_REFRESH_TOKEN=your-refresh-token-here

# Token expiration (in seconds, optional, defaults to 86400 = 24 hours)
MAXIMIZE_TOKEN_EXPIRES_IN=86400

# API Key for protecting your proxy
# Generate with: openssl rand -hex 32
# HIGHLY RECOMMENDED for production deployments
MAXIMIZE_API_KEY=your-secure-random-api-key-here

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Server bind address (default: 0.0.0.0)
# MAXIMIZE_BIND_ADDRESS=0.0.0.0

# Server port (default: 8081)
# MAXIMIZE_PORT=8081

# Log level: debug, info, warn, error (default: info)
# RUST_LOG=info

# =============================================================================
# USAGE EXAMPLES
# =============================================================================

# Local development (CLI mode):
#   cargo run
#   or: ./target/release/maximize

# Server-only mode (production):
#   cargo run -- --server-only
#   or: ./target/release/maximize --server-only

# With custom bind address:
#   ./target/release/maximize --server-only --bind 127.0.0.1

# Docker:
#   docker run -d --env-file .env -p 8081:8081 maximize:latest

# =============================================================================
# SECURITY NOTES
# =============================================================================

# 1. NEVER commit .env file to git (it's in .gitignore)
# 2. Always use MAXIMIZE_API_KEY in production
# 3. Use HTTPS in production (via reverse proxy or CapRover)
# 4. Rotate tokens periodically
# 5. Keep this file secure with proper file permissions:
#    chmod 600 .env
